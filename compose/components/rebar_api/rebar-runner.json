{
  "service": {
    "name": "rebar-job-runner-service",
    "tags": [ "system" ],
    "port": 3000,
    "check": {
      "script": "ps aux | grep que:work | grep -v grep",
      "interval": "10s"
    }
  }
}
