---
- Name: 'test logging rule'
  WantsAttribs:
    - number_of_drives
  Matchers:
    - Enabled: true
    - JSON: 
        Selector: ':root .Attribs .number_of_drives:val(5)'
        SaveAs: 'driveCount'
  MatchActions:
    - Log: true
    - Script: |
       echo "System has 5 drives"
       env
