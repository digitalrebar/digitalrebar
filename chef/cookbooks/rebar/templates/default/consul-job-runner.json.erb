{
  "service": {
    "name": "rebar-job-runner-service",
    "tags": [ "system" ],
    <% if @ip_addr -%>
    "address": "<%= @ip_addr %>",
    <% end -%>
    "port": 3000,
    "check": {
      "script": "ps aux | grep que:work | grep -v grep",
      "interval": "10s"
    }
  }
}
